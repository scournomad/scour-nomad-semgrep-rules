rules:
  - id: dotnet-authn-requirehttpsmetadata-false
    languages: [csharp]
    message: RequireHttpsMetadata=false in production is insecure. Enable HTTPS metadata fetching for OpenID/JWT bearer.
    severity: WARNING
    patterns:
      - pattern-either:
          - pattern: options.RequireHttpsMetadata = false
    metadata:
      cwe: "CWE-319: Cleartext Transmission of Sensitive Information"
      owasp: "A02:2021-Cryptographic Failures"
      severity: "Medium"
      risk_score: null
      confidence: "Low"
      impact: "Fetching OpenID/JWT metadata over HTTP can be intercepted or tampered with, enabling token forgery or config spoofing."
      recommendation: "Set RequireHttpsMetadata=true in production and ensure authority endpoints use HTTPS only."
      references:
        - https://learn.microsoft.com/aspnet/core/security/authentication/jwt
      tags:
        - authn
        - CWE-319
        - A02:2021
        - WASC-04
      verification:
        strategy: manual
        steps:
          - Inspect environment; ensure HTTPS in prod
        expected_outcome: Insecure metadata retrieval allowed
        tooling:
          unit: none
          dast: none
