rules:
  - id: dotnet-jwt-signature-validation-disabled
    languages: [csharp]
    message: JWT signature validation disabled. Enable ValidateIssuerSigningKey/RequireSignedTokens.
    severity: ERROR
    patterns:
      - pattern-inside: |
          new TokenValidationParameters { $OPTS }
      - pattern-either:
          - pattern: ValidateIssuerSigningKey = false
          - pattern: RequireSignedTokens = false
    metadata:
      cwe: "CWE-347: Improper Verification of Cryptographic Signature"
      owasp: "A07:2021-Identification and Authentication Failures"
      severity: "High"
      risk_score: null
      confidence: "Medium"
      impact: "Disabling signature validation allows forged or tampered tokens to be accepted, enabling authentication bypass."
      recommendation: "Enable ValidateIssuerSigningKey and RequireSignedTokens; configure valid signing keys and algorithms."
      references:
        - https://learn.microsoft.com/aspnet/core/security/authentication/jwt
      tags:
        - "jwt"
        - "CWE-347"
        - "A07:2021"
        - "WASC-01"
      verification:
        strategy: unit
        steps:
          - Configure TokenValidationParameters with signature validation disabled
        expected_outcome: Unsigned/invalid-signed tokens are accepted
        tooling:
          unit: xunit
          dast: none
