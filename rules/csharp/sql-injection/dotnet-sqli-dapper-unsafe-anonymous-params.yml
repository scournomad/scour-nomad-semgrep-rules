rules:
  - id: dotnet-sqli-dapper-unsafe-anonymous-params
    languages: [csharp]
    message: Potential SQL injection. Dapper query built via concat before passing anonymous params object.
    severity: WARNING
    patterns:
      - pattern-either:
        - pattern: $Q = $A + $B;
        - pattern: $Q += $X;
        - pattern: $VAR $Q = $A + $B;

        # interpolation: assign to Q via interpolated string
        - pattern: $Q = $"...{$X}...";
        - pattern: $VAR $Q = $"...{$X}...";

        # string.Format variants
        - pattern: $Q = string.Format($FMT, ...);
        - pattern: $VAR $Q = string.Format($FMT, ...);

        # string.Concat variants
        - pattern: $Q = string.Concat(...);
        - pattern: $VAR $Q = string.Concat(...);

        # optional: FormattableString (less common)
        - pattern: $Q = FormattableStringFactory.Create($FMT, ...);
        - pattern: $VAR $Q = FormattableStringFactory.Create($FMT, ...);
      - pattern-either:
        - pattern: $CONN.Query($Q, $PARAMS)
        - pattern: $CONN.Query<...>($Q, $PARAMS)

    metadata:
      cwe: "CWE-89: SQL Injection"
      owasp: "A03:2021-Injection"
      severity: "High"
      risk_score: ""
      confidence: "Low"
      impact: "Building SQL strings via concatenation may allow attackers to alter queries despite parameter objects, leading to data exfiltration or modification."
      recommendation: "Use parameter placeholders in the SQL string itself (e.g., @name) and pass values via parameters; avoid any string concatenation or interpolation."
      references:
        - https://github.com/DapperLib/Dapper#parameterization
        - https://cheatsheetseries.owasp.org/cheatsheets/SQL_Injection_Prevention_Cheat_Sheet.html
      tags:
        - sql-injection
        - CWE-89
        - A03:2021
        - WASC-19
      verification:
        strategy: integration
        steps:
          - Pass tainted value into concatenated SQL using anonymous params
        expected_outcome: Value appears unescaped in SQL
        tooling:
          unit: xunit
          dast: none
