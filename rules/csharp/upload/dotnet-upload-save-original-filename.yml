rules:
  - id: dotnet-upload-save-original-filename
    languages: [csharp]
    message: Saving uploads using untrusted original filename can lead to path traversal or overwrites. Sanitize or generate names.
    severity: WARNING
    patterns:
      - pattern-either:
          - pattern: Path.Combine($DIR, $FILE.FileName)
          - pattern: $PATH = $FILE.FileName
    metadata:
      cwe: "CWE-22: Path Traversal"
      owasp: "A01:2021-Broken Access Control"
      severity: "Medium"
      risk_score: null
      confidence: "Low"
      impact: "Using the original filename can enable traversal or overwrite existing files, leading to data loss or compromise."
      recommendation: "Sanitize with Path.GetFileName and generate server-side unique names; enforce allowed extensions and directories."
      references:
        - https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html
      tags:
        - upload
        - CWE-22
        - A01:2021
        - WASC-33
      verification:
        strategy: unit
        steps:
          - Save IFormFile using FileName without sanitization
        expected_outcome: Dangerous file path construction
        tooling:
          unit: xunit
          dast: none
