rules:
  - id: dotnet-regex-match-timeout-unset-static
    languages: [csharp]
    message: Regex static methods without explicit timeout may allow ReDoS. Provide a timeout.
    severity: WARNING
    patterns:
      - pattern-either:
          - pattern: Regex.IsMatch($S, $R)
          - pattern: Regex.Match($S, $R)
          - pattern: Regex.Matches($S, $R)
    metadata:
      cwe: "CWE-1333: Inefficient Regular Expression Complexity"
      owasp: "A05:2021-Security Misconfiguration"
      severity: "Medium"
      risk_score: ""
      confidence: "Low"
      impact: "Complex regex without timeouts may cause catastrophic backtracking and CPU exhaustion (ReDoS)."
      recommendation: "Use overloads that include a timeout or set a global default; simplify patterns to avoid catastrophic backtracking."
      references:
        - https://learn.microsoft.com/dotnet/standard/base-types/best-practices#use-timeout-values
      tags:
        - regex
        - CWE-1333
        - A05:2021
        - WASC-10
      verification:
        strategy: unit
        steps:
          - Use complex regex without timeout
        expected_outcome: Potential catastrophic backtracking
        tooling:
          unit: xunit
          dast: none
