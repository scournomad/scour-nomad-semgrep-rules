rules:
  - id: dotnet-ssrf-httpclient-user-controlled-url
    languages: [csharp]
    message: Potential SSRF. HttpClient invoked with variable URL; validate/allowlist destinations.
    severity: WARNING
    patterns:
      - pattern-either:
          - pattern: $C.GetAsync($URL, ...)
          - pattern: $C.PostAsync($URL, ...)
          - pattern: $C.SendAsync($REQ)
    metadata:
      cwe: "CWE-918: Server-Side Request Forgery (SSRF)"
      owasp: "A10:2021-Server-Side Request Forgery (SSRF)"
      severity: "Medium"
      risk_score: null
      confidence: "Low"
      impact: "Unvalidated URLs may let attackers pivot server-side to internal services or metadata endpoints."
      recommendation: "Validate and allowlist outbound destinations. Block link-local/reserved IP ranges and sensitive endpoints."
      references:
        - https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html
      tags:
        - ssrf
        - CWE-918
        - A10:2021
        - WASC-20
      verification:
        strategy: manual
        steps:
          - Trace provenance of $URL; if user-controlled, confirm SSRF risk
        expected_outcome: Variable URL must be validated/allowlisted
        tooling:
          unit: none
          dast: none
