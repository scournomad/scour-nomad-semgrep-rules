rules:
  - id: dotnet-logging-exception-to-client
    languages: [csharp]
    message: Returning exception details (ex.ToString()) to client leaks sensitive info.
    severity: WARNING
    patterns:
      - pattern-either:
          - pattern: return Content($EX.ToString(), ...)
          - pattern: return Problem($EX.ToString(), ...)
          - pattern: await Response.WriteAsync($EX.ToString(), ...)
    metadata:
      cwe: "CWE-209: Information Exposure Through an Error Message"
      owasp: "A09:2021-Security Logging and Monitoring Failures"
      severity: "Medium"
      risk_score: ""
      confidence: "Low"
      impact: "Detailed exceptions may expose stack traces, file paths, queries, or secrets, aiding attackers."
      recommendation: "Return generic error messages to clients and log detailed exceptions server-side only."
      references:
        - https://learn.microsoft.com/aspnet/core/fundamentals/error-handling
      tags:
        - logging
        - CWE-209
        - A09:2021
        - WASC-13
      verification:
        strategy: integration
        steps:
          - Trigger exception and inspect response body
        expected_outcome: Stack trace or details exposed
        tooling:
          unit: none
          dast: curl
