rules:
  - id: dotnet-secrets-hardcoded-connectionstring
    languages: [csharp]
    message: Hardcoded database connection string detected. Store secrets securely (e.g., env vars, Key Vault).
    severity: ERROR
    pattern-either:
      - pattern: string $S = "Server=...;...Password=...;..." 
      - pattern: var $S = "Server=...;...Password=...;..."
    metadata:
      cwe: "CWE-798: Use of Hard-coded Credentials"
      owasp: "A02:2021-Cryptographic Failures"
      severity: "High"
      risk_score: null
      confidence: "Medium"
      impact: "Credentials in source control can be exfiltrated and abused to access databases and move laterally."
      recommendation: "Remove hardcoded secrets. Use secure secret stores (env vars, Key Vault, AWS/GCP secrets) and rotate compromised credentials."
      references:
        - https://owasp.org/www-community/vulnerabilities/Use_of_hard-coded_credentials
      tags:
        - secrets
        - CWE-798
        - A02:2021
        - WASC-04
      verification:
        strategy: manual
        steps:
          - Inspect string literal; confirm it contains sensitive credentials
        expected_outcome: Sensitive connection string in source code
        tooling:
          unit: none
          dast: none
