rules:
  - id: dotnet-secrets-hardcoded-jwt-key
    languages: [csharp]
    message: Hardcoded JWT signing key detected. Store keys securely and rotate.
    severity: ERROR
    patterns:
      - pattern-either:
          - pattern: new SymmetricSecurityKey(Encoding.$ENC.GetBytes("..."))
          - pattern: new SymmetricSecurityKey(Convert.FromBase64String("..."))
    metadata:
      cwe: "CWE-321: Use of Hard-coded Cryptographic Key"
      owasp: "A02:2021-Cryptographic Failures"
      severity: "High"
      risk_score: null
      confidence: "Medium"
      impact: "Leaked signing keys allow forging JWTs, enabling authentication bypass and privilege escalation."
      recommendation: "Remove hardcoded keys. Load from a secure key vault or environment and rotate on exposure."
      references:
        - https://owasp.org/www-community/vulnerabilities/Use_of_hard-coded_cryptographic_key
      tags:
        - secrets
        - CWE-321
        - A02:2021
        - WASC-04
      verification:
        strategy: unit
        steps:
          - Inspect for literal key material
        expected_outcome: Hardcoded key present
        tooling:
          unit: xunit
          dast: none
